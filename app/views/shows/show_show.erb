<h1>Show <%= @show.id %> Details</h1>

Date: <%= @show.date.to_date.strftime("%A, %B %d, %Y") %> </p>

Venue: <%= @show.venue.name %></p>

Artists: <%= @show.artists.collect { |artist| artist.name }.join(" + ") %></p>

URL: <%= @show.url %></p>
