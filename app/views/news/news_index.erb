<h2>News</h2>

<table>
  <tr>
  <% @news.each do |news| %>
    <td>
      <% if !news.date.nil? && !news.date.empty? %>
      <%= news.date.to_date.strftime("%m.%d") %>
      <% end %>
    </td>

    <td class="details">
      <%= news.content %>
    </td>
  </tr>
  <% end %>
</table>
