<h2>Edit User</h2>

<form method="POST" action="/settings">
<input id="hidden" type="hidden" name="_method" value="patch">

  <p>
    <% if @errors && @errors[:username] %>
      <label for="username" style="color: violet;">
        <%= @errors[:username] %>
      </label>
    <% else %>
      <label for="username">
        Username:
      </label>
    <% end %>
    <input
      type="text"
      name="username"
      id="username"
      value="<%= current_user.username %>"
      autocomplete="off"
      />
  </p>

  <p>
    <% if @errors && @errors[:email] %>
      <label for="email" style="color: violet;">
        <%= @errors[:email] %>
      </label>
    <% else %>
      <label for="email">
        Email:
      </label>
    <% end %>
    <input
      type="email"
      name="email"
      id="email"
      value="<%= current_user.email %>"
      autocomplete="off"
      />
  </p>

  <p>
    <% if @errors && @errors[:password] %>
      <label for="password" style="color: violet;">
        <%= @errors[:password] %>
      </label>
    <% else %>
      <label for="password">
        Enter Password To Make Changes:
      </label>
    <% end %>
    <input
      type="password"
      name="password"
      id="password"
      />
  </p>

  <input type="submit" value="UPDATE" />
</form>

<p></p>

<h2>Change Password</h2>

<form method="POST" action="/password">
  <input id="hidden" type="hidden" name="_method" value="patch">

  <p>
    <% if @errors && @errors[:old_password] %>
      <label for="old_password" style="color: violet;">
        <%= @errors[:old_password] %>
      </label>
    <% else %>
      <label for="old_password">
        Old Password:
      </label>
    <% end %>
    <input
      type="password"
      name="old_password"
      id="old_password"
      />
  </p>

  <p>
    <% if @errors && @errors[:new_password] %>
      <label for="new_password" style="color: violet;">
        <%= @errors[:new_password] %>
      </label>
    <% else %>
      <label for="new_password">
        New Password:
      </label>
    <% end %>
    <input
      type="password"
      name="new_password"
      id="new_password"
      />
  </p>

  <p>
    <% if @errors && @errors[:confirm_password] %>
      <label for="confirm_password" style="color: violet;">
        <%= @errors[:confirm_password] %>
      </label>
    <% else %>
      <label for="confirm_password">
        Confirm New Password:
      </label>
    <% end %>
    <input
      type="password"
      name="confirm_password"
      id="confirm_password"
      />
  </p>

  <input type="submit" value="SUBMIT" />
</form>

<p></p>

<h2>Delete Account</h2>

<form method="POST" action="/users/<%= @user.id %>/delete">
  <input id="hidden" type="hidden" name="_method" value="delete">

  <p>
    <label for="delete_password">
      Enter Password to Delete User:
    </label>
    <input
      type="password"
      name="password"
      id="delete_password"
      required />
  </p>

  <input type="submit" value="DELETE USER"></p>
</form>

<p></p>
